function sparse_analyze(ast, lives, loop_info, invariants)
  dprintln(2, "sparse_analyze: ", ast, " ", lives, " ", loop_info, " ", invariants)
  nothing # This should be replaced with the new function symbol.  
end
